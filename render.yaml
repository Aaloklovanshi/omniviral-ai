services:
  - type: web
    name: omniviral-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_server.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false
